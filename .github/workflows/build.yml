name: Build Snow Linux

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

env:
  IMAGE_NAME: snosi
  REPO_NAME: snosi
  IMAGE_DESC: "Snow Linux - A minimal bootc-enabled OS image"
  IMAGE_KEYWORDS: "linux,os,bootc,systemd,debian"
  IMAGE_LICENSE: Apache-2.0
  IMAGE_LOGO_URL: "" # Optional: URL to logo image

concurrency:
  group: ${{ github.workflow }}-${{ github.ref || github.run_id }}
  cancel-in-progress: ${{ github.event_name == 'push' }}

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
      id-token: write
      attestations: write

    steps:
      - name: Aggressive cleanup
        run: |
          # Remove Java (JDKs)
          sudo rm -rf /usr/lib/jvm

          # Remove .NET SDKs
          sudo rm -rf /usr/share/dotnet

          # Remove Swift toolchain
          sudo rm -rf /usr/share/swift

          # Remove Haskell (GHC)
          sudo rm -rf /usr/local/.ghcup

          # Remove Julia
          sudo rm -rf /usr/local/julia*

          # Remove Android SDKs
          sudo rm -rf /usr/local/lib/android

          # Remove Chromium (optional if not using for browser tests)
          sudo rm -rf /usr/local/share/chromium

          # Remove Microsoft/Edge and Google Chrome builds
          sudo rm -rf /opt/microsoft /opt/google

          # Remove Azure CLI
          sudo rm -rf /opt/az

          # Remove PowerShell
          sudo rm -rf /usr/local/share/powershell

          # Remove CodeQL and other toolcaches
          sudo rm -rf /opt/hostedtoolcache

          docker system prune -af || true
          docker builder prune -af || true
          df -h
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Generate build date
        id: date
        run: echo "date=$(date -u +%Y-%m-%dT%H:%M:%SZ)" >> $GITHUB_OUTPUT

      - name: Generate version tag
        id: version
        run: echo "tag=1.0.$(date -u +%Y%m%d)" >> $GITHUB_OUTPUT

      - name: setup-mkosi
        uses: systemd/mkosi@main

      - name: Build main and sysexts
        env:
          # These are picked up by mkosi.postoutput script
          CREATED_DATE: ${{ steps.date.outputs.date }}
          DEFAULT_TAG: ${{ steps.version.outputs.tag }}
          IMAGE_NAME: ${{ env.IMAGE_NAME }}
          IMAGE_DESC: ${{ env.IMAGE_DESC }}
          IMAGE_KEYWORDS: ${{ env.IMAGE_KEYWORDS }}
          IMAGE_LICENSE: ${{ env.IMAGE_LICENSE }}
          IMAGE_LOGO_URL: ${{ env.IMAGE_LOGO_URL }}
          # These are automatically available from GitHub context
          GITHUB_SHA: ${{ github.sha }}
          GITHUB_REPOSITORY_OWNER: ${{ github.repository_owner }}
        run: |
          sudo -E mkosi build
          sudo ./sysextmv.sh
      - name: Publish sysexts to frostyard repo
        if: github.event_name != 'pull_request'
        uses: frostyard/repogen/.github/actions/publish-to-r2@main
        with:
          r2-account-id: ${{ secrets.R2_ACCOUNT_ID }}
          r2-access-key-id: ${{ secrets.R2_ACCESS_KEY_ID }}
          r2-secret-access-key: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          r2-bucket: frostyardrepo
          skip-duplicates: true
          gpg-private-key: ${{ secrets.REPOGEN_GPG_KEY }}
          packages-dir: ./output/sysexts
          package-type: sysext # or sysext
          base-url: https://repository.frostyard.org # required for sysext
      - name: Build snow
        env:
          # These are picked up by mkosi.postoutput script
          CREATED_DATE: ${{ steps.date.outputs.date }}
          DEFAULT_TAG: ${{ steps.version.outputs.tag }}
          IMAGE_NAME: snow
          IMAGE_DESC: ${{ env.IMAGE_DESC }}
          IMAGE_KEYWORDS: ${{ env.IMAGE_KEYWORDS }}
          IMAGE_LICENSE: ${{ env.IMAGE_LICENSE }}
          IMAGE_LOGO_URL: ${{ env.IMAGE_LOGO_URL }}
          # These are automatically available from GitHub context
          GITHUB_SHA: ${{ github.sha }}
          GITHUB_REPOSITORY_OWNER: ${{ github.repository_owner }}
        run: |
          sudo -E mkosi --profile 10-image-snow --profile 20-kernel-backports --profile 80-finalize-bootc --profile 90-output-oci build

      - name: Login to GitHub Container Registry
        if: github.event_name != 'pull_request'
        run: skopeo login ghcr.io -u ${{ github.actor }} --password ${{ secrets.GHCR_PAT }}
        env:
          GITHUB_TOKEN: ${{ secrets.GHCR_PAT }}

      - name: Push snow to registry
        if: github.event_name != 'pull_request'
        env:
          REGISTRY: ghcr.io
          IMAGE_NAME: snow
          IMAGE: ghcr.io/${{ github.repository_owner }}/snosi-${{ env.IMAGE_NAME }}
        run: |
          # Push with version tag
          skopeo copy \
            --dest-creds="${{ github.actor }}:${{ secrets.GHCR_PAT }}" \
            oci:output/snow \
            docker://${IMAGE}:${{ steps.version.outputs.tag }}
          # Push latest tag
          skopeo copy \
            --dest-creds="${{ github.actor }}:${{ secrets.GHCR_PAT }}" \
            oci:output/snow \
            docker://${IMAGE}:latest
      - name: cleanup snow build output
        run: |
          sudo -E mkosi clean
      - name: Build snow loaded
        env:
          # These are picked up by mkosi.postoutput script
          CREATED_DATE: ${{ steps.date.outputs.date }}
          DEFAULT_TAG: ${{ steps.version.outputs.tag }}
          IMAGE_NAME: snow-loaded
          IMAGE_DESC: ${{ env.IMAGE_DESC }}
          IMAGE_KEYWORDS: ${{ env.IMAGE_KEYWORDS }}
          IMAGE_LICENSE: ${{ env.IMAGE_LICENSE }}
          IMAGE_LOGO_URL: ${{ env.IMAGE_LOGO_URL }}
          # These are automatically available from GitHub context
          GITHUB_SHA: ${{ github.sha }}
          GITHUB_REPOSITORY_OWNER: ${{ github.repository_owner }}
        run: |
          sudo -E mkosi --profile 10-image-snow --profile 20-kernel-backports --profile 30-packages-loaded --profile 40-name-snowloaded --profile 80-finalize-bootc --profile 90-output-oci build
      - name: Push snowloaded to registry
        if: github.event_name != 'pull_request'
        env:
          REGISTRY: ghcr.io
          IMAGE_NAME: snow-loaded
          IMAGE: ghcr.io/${{ github.repository_owner }}/snosi-${{ env.IMAGE_NAME }}
        run: |
          # Push with version tag
          skopeo copy \
            --dest-creds="${{ github.actor }}:${{ secrets.GHCR_PAT }}" \
            oci:output/snowloaded \
            docker://${IMAGE}:${{ steps.version.outputs.tag }}
          # Push latest tag
          skopeo copy \
            --dest-creds="${{ github.actor }}:${{ secrets.GHCR_PAT }}" \
            oci:output/snowloaded \
            docker://${IMAGE}:latest
      - name: cleanup snowloaded build output
        run: |
          sudo -E mkosi clean
      - name: Build snowfield
        env:
          # These are picked up by mkosi.postoutput script
          CREATED_DATE: ${{ steps.date.outputs.date }}
          DEFAULT_TAG: ${{ steps.version.outputs.tag }}
          IMAGE_NAME: snowfield
          IMAGE_DESC: ${{ env.IMAGE_DESC }}
          IMAGE_KEYWORDS: ${{ env.IMAGE_KEYWORDS }}
          IMAGE_LICENSE: ${{ env.IMAGE_LICENSE }}
          IMAGE_LOGO_URL: ${{ env.IMAGE_LOGO_URL }}
          # These are automatically available from GitHub context
          GITHUB_SHA: ${{ github.sha }}
          GITHUB_REPOSITORY_OWNER: ${{ github.repository_owner }}
        run: |
          sudo -E mkosi --profile 10-image-snow --profile 20-kernel-surface --profile 40-name-snowfield --profile 80-finalize-bootc --profile 90-output-oci build
      - name: Push snowfield to registry
        if: github.event_name != 'pull_request'
        env:
          REGISTRY: ghcr.io
          IMAGE_NAME: snowfield
          IMAGE: ghcr.io/${{ github.repository_owner }}/snosi-${{ env.IMAGE_NAME }}
        run: |
          # Push with version tag
          skopeo copy \
            --dest-creds="${{ github.actor }}:${{ secrets.GHCR_PAT }}" \
            oci:output/snowfield \
            docker://${IMAGE}:${{ steps.version.outputs.tag }}
          # Push latest tag
          skopeo copy \
            --dest-creds="${{ github.actor }}:${{ secrets.GHCR_PAT }}" \
            oci:output/snowfield \
            docker://${IMAGE}:latest
      - name: cleanup snowfield build output
        run: |
          sudo -E mkosi clean
      - name: Build snowfieldloaded
        env:
          # These are picked up by mkosi.postoutput script
          CREATED_DATE: ${{ steps.date.outputs.date }}
          DEFAULT_TAG: ${{ steps.version.outputs.tag }}
          IMAGE_NAME: snowfieldloaded
          IMAGE_DESC: ${{ env.IMAGE_DESC }}
          IMAGE_KEYWORDS: ${{ env.IMAGE_KEYWORDS }}
          IMAGE_LICENSE: ${{ env.IMAGE_LICENSE }}
          IMAGE_LOGO_URL: ${{ env.IMAGE_LOGO_URL }}
          # These are automatically available from GitHub context
          GITHUB_SHA: ${{ github.sha }}
          GITHUB_REPOSITORY_OWNER: ${{ github.repository_owner }}
        run: |
          sudo -E mkosi --profile 10-image-snow --profile 20-kernel-surface --profile 30-packages-loaded --profile 40-name-snowfieldloaded --profile 80-finalize-bootc --profile 90-output-oci build
      - name: Push snowfieldloaded to registry
        if: github.event_name != 'pull_request'
        env:
          REGISTRY: ghcr.io
          IMAGE_NAME: snowfield-loaded
          IMAGE: ghcr.io/${{ github.repository_owner }}/snosi-${{ env.IMAGE_NAME }}
        run: |
          # Push with version tag
          skopeo copy \
            --dest-creds="${{ github.actor }}:${{ secrets.GHCR_PAT }}" \
            oci:output/snowfieldloaded \
            docker://${IMAGE}:${{ steps.version.outputs.tag }}
          # Push latest tag
          skopeo copy \
            --dest-creds="${{ github.actor }}:${{ secrets.GHCR_PAT }}" \
            oci:output/snowfieldloaded \
            docker://${IMAGE}:latest
