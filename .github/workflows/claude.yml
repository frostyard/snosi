name: Claude AI Integration

on:
  workflow_dispatch:

permissions: {}

jobs:
  integration-marker:
    runs-on: ubuntu-latest
    steps:
      - name: Document active AI feedback loops
        run: |
          set -euo pipefail
          echo "ACMM GitHub Actions AI integration marker."
          echo "The operational AI issue handoff lives in ai-fix-requested.yml."
