# dev sysext: build toolchain. Entries verified against the CI build log's
# "Unpacking ..." lines — gcc, g++, and make are satisfied by the BASE image
# layer and never land in this sysext's delta; cmake/gdb/ninja/automake do.
# automake also exercises the postinst alternatives-symlink repoint.
/usr/bin/cmake
/usr/bin/gdb
/usr/bin/ninja
/usr/bin/automake
# GTK4/WebKitGTK development headers (libgtk-4-dev, libwebkitgtk-6.0-dev) —
# -dev packages are not in base, so their include trees must be in the delta.
/usr/include/gtk-4.0
/usr/include/webkitgtk-6.0
# Runtime libraries the GTK4/WebKitGTK dev stack links but that base does NOT
# provide. Until #771 (2026-08-25) base transitively carried a whole desktop,
# so the delta published 2026-08-07 omitted these as "already in the base" and
# they vanished from cayo when the base shrank. A sysext delta omits anything
# its BUILD BASE has, so listing them in Packages= would not help — only this
# check, which runs against the delta, can assert they ship. See
# docs/design/sysexts.md "A Sysext Delta Is Only Valid Against the Base It Was
# Built On".
/usr/lib/x86_64-linux-gnu/libepoxy.so.0
/usr/lib/x86_64-linux-gnu/libgdk_pixbuf-2.0.so.0
/usr/lib/x86_64-linux-gnu/libgstreamer-1.0.so.0
/usr/lib/x86_64-linux-gnu/libpango-1.0.so.0
