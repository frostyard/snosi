# ydotoold types through /dev/uinput, whose module is not loaded by default
# (the node exists only as a kmod static node at root:root 0600). Loading it
# here — after snosi-sysext-udev-reload has re-read the sysext's
# 80-uinput.rules — is what gets the node to root:input 0660.
uinput
