[Config]
Dependencies=
Dependencies=base

[Output]
ManifestFormat=json
ImageId=sundog
Output=sundog

[Include]
# bootc deployment runtime
Include=%D/shared/packages/bootc/mkosi.conf
# bootc-only secure package, trust, and immutable-karg composition
Include=%D/shared/bootc-secure/mkosi.conf
# sundog payload composition (tree, scripts, packages)
Include=%D/shared/composition/sundog/mkosi.conf
# backports kernel
Include=%D/shared/kernel/backports/mkosi.conf
# Image Output
Include=%D/shared/outformat/image/mkosi.conf
