[Content]
# Reuse the established bootc system policy and factory-state scaffolding;
# the Sundog tree overlays desktop-specific SDDM, Plymouth, and user policy.
ExtraTrees=%D/shared/cayo/tree
ExtraTrees=%D/shared/desktop/tree
ExtraTrees=%D/shared/sundog/tree

# Sundog is bootc-only. bootc supplies rhgb/quiet at install time, so these
# splash kargs are intentionally not duplicated as KernelCommandLine= here.

# brew build script
BuildScripts=%D/shared/scripts/build/brew.chroot
# dracut
PostInstallationScripts=%D/shared/kernel/scripts/postinst/mkosi.postinst.chroot
# image customizations
PostInstallationScripts=%D/shared/sundog/scripts/postinstall/sundog.postinst.chroot
# manifest postoutput
PostOutputScripts=%D/shared/manifest/postoutput/mkosi.postoutput
# image finalize
FinalizeScripts=%D/shared/outformat/image/finalize/mkosi.finalize.chroot
FinalizeScripts=%D/shared/composition/var-audit.finalize

[Include]
# sundog packages
Include=%D/shared/packages/sundog/mkosi.conf
