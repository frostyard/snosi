#!/bin/bash

# omarchy:summary=Install packages via picker (snosi: guidance only)

# snosi override: see shared/flurry/omarchy-overrides/ in the snosi repo.
source /usr/share/omarchy/bin/omarchy-snosi-lib
snosi_immutable "Packages cannot be installed into the immutable image."
exit 1
