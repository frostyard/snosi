# SDDM state directory (last-session/last-user hints). Debian's sddm ships
# /var/lib/sddm in the package payload, not via tmpfiles; recreate it so a
# fresh installer-created /var still has it.
d /var/lib/sddm 0750 sddm sddm - -
