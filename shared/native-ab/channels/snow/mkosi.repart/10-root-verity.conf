[Partition]
Type=root-verity
Label=snow_%A_v
Verity=hash
VerityMatchKey=root
# PROVISIONAL (docs/native-ab-contracts.md §12): scaled from the
# verity:root ratio (1/32 of slot, rounded up to a power-of-two MiB value)
# against this product's 8G root slot (8192M/32 = 256M, already a power of
# two). Finalized in Phase 5/6. See docs/native-ab-capacities.md.
SizeMinBytes=256M
SizeMaxBytes=256M
SplitName=snow_@v.root-verity.raw
