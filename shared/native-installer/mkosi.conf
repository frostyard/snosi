# SPDX-License-Identifier: LGPL-2.1-or-later
#
# Content fragment for mkosi.profiles/native-installer (docs/native-ab-contracts.md
# §8, "Installer ISO boot chain"; docs/plans/2026-07-14-bootc-native-ab-coexistence-
# plan.md "Installer ISO"). Payload-free network-installer environment: no Snow,
# Snowfield, or Cayo content. The ISO itself boots through a fully Debian-trusted
# pre-enrollment Secure Boot chain (Microsoft db -> Debian-signed shim -> Debian-
# signed GRUB -> Debian-signed stock kernel), assembled entirely OUTSIDE mkosi by
# shared/native-installer/tools/build-iso.sh (mkosi has no ISO/El Torito output
# format -- checked the pinned man page's Format= enumeration). This fragment only
# produces the plain rootfs directory that script packs into the ISO's ESP and
# initramfs.
#
# Bootable=no and no ShimBootloader=/SecureBoot=/UnifiedKernelImages= here:
# mkosi's own UKI/systemd-boot signing pipeline is not used at all for this
# profile. The kernel that actually boots is Debian's OWN Secure-Boot-signed
# stock package (see shared/native-installer/tools/build-iso.sh), not anything
# mkosi builds or signs.

[Output]
Format=directory
ManifestFormat=json

[Build]
# Forky sandbox reused verbatim from the production native-ab secure fragment
# (same directory, not copied) -- docs/native-ab-contracts.md §8 requires a
# coherent Forky systemd 261 cryptsetup/TPM family in the installer userspace
# even though the boot KERNEL stays Debian-signed stock trixie.
SandboxTrees=%D/shared/native-ab-secure/package-manager

[Content]
Bootable=no
CleanPackageMetadata=yes
WithDocs=no

# Pre-seed first-boot identity at BUILD time (mkosi runs systemd-firstboot
# itself and writes /etc/{locale.conf,localtime,hostname,shadow} directly)
# so systemd-firstboot.service has nothing left to ask at runtime.
# Root-caused live: with these unset, systemd-firstboot.service blocks the
# WHOLE boot indefinitely on an interactive timezone prompt on the serial
# console -- no TTY is feeding it an answer, so it hangs forever (looked
# exactly like a silent kernel/systemd deadlock until traced to this).
# RootPassword=hashed: (colon, no hash after it) is mkosi's documented way
# to create an UNLOCKED account with no password -- appropriate here: SSH
# access is still gated on PermitRootLogin prohibit-password (Debian's
# default, unchanged) plus the installer's own injected authorized_keys,
# so this only affects local serial/console login, exactly the "serial
# login" path docs/native-ab-contracts.md §8 names as a boot-chain proof
# path alongside SSH.
Locale=C.UTF-8
Keymap=us
Timezone=UTC
Hostname=snosi-installer
RootPassword=hashed:

# Coherent Forky systemd 261 family (cryptsetup/TPM enrollment tooling must be
# 261, not trixie's older systemd -- contract §8, "257-created tokens are
# unsupported"). Deliberately excludes systemd-boot*/systemd-container: this
# profile never uses systemd-boot, and per-A/B-slot bootloader packages don't
# belong in a payload-free installer.
Packages=libnss-myhostname/forky
         libnss-mymachines/forky
         libnss-systemd/forky
         libpam-systemd/forky
         libsystemd-shared/forky
         libsystemd0/forky
         libudev1/forky
         systemd/forky
         systemd-cryptsetup/forky
         systemd-resolved/forky
         systemd-sysv/forky
         systemd-timesyncd/forky
         systemd-tpm/forky
         udev/forky

# Debian-trusted Secure Boot boot chain material -- stays on the trixie
# release (NOT forky) since these carry Debian's actual Secure Boot
# signatures for THIS release; shared/native-installer/tools/build-iso.sh
# pulls the signed binaries straight out of this built tree.
#   - shim-signed: EFI/BOOT/BOOTX64.EFI (Microsoft-signed).
#   - grub-efi-amd64-signed: the monolithic Debian-signed grub2 EFI image.
#   - shim-helpers-amd64-signed: MokManager (mmx64.efi) + the fallback
#     loader (fbx64.efi), Debian-signed. NOT shipped by shim-signed itself.
#   - linux-image-amd64: stock trixie kernel, confirmed Debian-signed
#     ("Debian Secure Boot Signer 2022 - linux" / "Debian Secure Boot CA",
#     verified via sbverify against the real .deb) -- not backports, whose
#     signed status was not independently verified for this task.
#   - mokutil: post-install MOK enrollment tool the CLI installer stages
#     (docs/plans/... "First-Round CLI Installer" step 16-17); also used by
#     the boot-chain proof itself (mokutil --sb-state inside the guest).
Packages=shim-signed
         grub-efi-amd64-signed
         shim-helpers-amd64-signed
         linux-image-amd64
         mokutil

# Networking (brief: NetworkManager-based, since it composes with the CLI
# installer step "start networking and synchronize time").
Packages=network-manager
         wpasupplicant
         iproute2
         iputils-ping
         openssh-server

# Storage tools for the CLI installer's disk-imaging step (matches the tool
# set test/cayo-ab-install-spike.sh already exercises: cryptsetup, mkfs.ext4
# from e2fsprogs). sfdisk was WRONGLY assumed to ship via util-linux
# (already essential, not listed explicitly) -- root-caused live
# (test/native-installer-e2e-test.sh's first real boot,
# 2026-07-15): on current Debian, `sfdisk`/`fdisk`/`cfdisk` were split out
# of util-linux into a separate `fdisk` package (confirmed via `dpkg -S
# /usr/sbin/sfdisk` -> `fdisk`, and via the built rootfs's own
# native-installer.manifest, which had util-linux but no fdisk package at
# all). Without it, `snosi-install`'s own startup tool-check
# (`for cmd in curl jq gpgv sfdisk ...`) died immediately with "required
# command not found: sfdisk" on every real boot -- the installer could
# never have partitioned a real disk. gdisk (a DIFFERENT GPT tool, kept
# separately) does not provide sfdisk and was never a substitute.
# openssl (the CLI, not just the libssl3 library many other packages
# already pull in transitively) is also required directly -- snosi-install
# uses `openssl rand -hex 32` (recovery passphrase generation) and
# `openssl x509 ... -outform DER` (MOK certificate conversion) -- and was
# not confirmed present by any other listed package's dependency chain, so
# added explicitly rather than risk a 3rd real-boot iteration to find out.
Packages=fdisk
         gdisk
         cryptsetup
         xz-utils
         e2fsprogs
         dosfstools
         openssl

# login(1) + /etc/pam.d/login for the serial/console root login path the
# RootPassword=hashed: comment above promises (contract §8 names serial login
# as a boot-chain proof path). Nothing else in this hand-picked set pulls it
# in: without it agetty prompts "login:", execs the missing /bin/login, and
# exits -- the getty respawns silently forever with no password prompt and no
# error (found on the first HUMAN console login attempt, 2026-07-16; the e2e
# harness only ever enters via SSH keys, so it never exercised this path).
Packages=login

# OpenPGP verification (signed index verification, contract §4) and TPM
# enrollment userspace. binutils (objcopy) is required by snosi-install's
# extract_pcrpkey_from_disk(): it dumps the .pcrpkey PE section straight out
# of the just-written disk's own UKI to enroll TPM unlock -- also missing
# entirely from this profile's original package list (same class of gap as
# the sfdisk/fdisk fix above, caught by the same real boot,
# test/native-installer-e2e-test.sh, 2026-07-15: "required command not
# found: objcopy" was the NEXT tool-check failure once sfdisk was fixed).
Packages=gpgv
         tpm2-tools
         binutils

# General installer utilities. ca-certificates is what makes curl able to
# verify TLS against the real https://repository.frostyard.org origin --
# nothing else in this hand-picked set ships the trust bundle
# (/etc/ssl/certs/ca-certificates.crt), and test/native-installer-e2e-test.sh
# could never catch its absence because its --origin is a plain-HTTP local
# fixture server (found on the first human install against the live published
# origin, 2026-07-16: `curl: (77) error setting certificate file`).
Packages=jq
         curl
         ca-certificates
# mount/umount live in the 'mount' package (separate from util-linux, whose
# agetty/lsblk/etc. ARE present). snosi-install mounts the ESP to read the
# just-written UKI (extract_pcrpkey_from_disk) and mounts var for seeding.
# It WAS transitively present when test/native-installer-e2e-test.sh went
# 75/75 (2026-07-15) and had vanished from a rebuild one day later ("mount:
# command not found" mid-install, first human run 2026-07-16) -- a moving
# forky-suite dependency shuffle, the same class of drift that dropped
# systemd-measure from the tools tree. Anything this script executes must be
# listed EXPLICITLY, never assumed from a dependency chain; the startup
# tool-check in snosi-install now also covers it.
Packages=mount

# Hardware firmware for network installs on real machines (not virtio-only
# QEMU fixtures): the installer boots on whatever NIC/storage controller the
# target machine has, before any product-specific package set is involved,
# so it needs its own copy of the same firmware policy the product images
# ship (shared/kernel/stock/mkosi.conf's firmware-* list, mirrored here --
# this profile deliberately has no Dependencies= on any product/kernel
# fragment, so it cannot just Include= that file without pulling in the
# stock kernel package/module machinery this profile doesn't want) plus the
# network-adapter-specific extras from shared/packages/snow and
# shared/packages/cayo (firmware-ath9k-htc/-carl9170/-zd1211 for USB Wi-Fi
# dongles, firmware-ast for server BMC/IPMI KVM video consoles). GPU/mesa
# rendering packages are deliberately excluded -- this is a text/SSH
# installer with no display server, so display firmware alone (already
# covered by firmware-amd-graphics/-intel-graphics/-nvidia-graphics/-cirrus/
# -ast above) is enough to keep BMC/laptop consoles usable, without the
# rendering userspace stack.
Packages=firmware-linux-free
         firmware-linux-nonfree
         firmware-linux
         firmware-amd-graphics
         firmware-atheros
         firmware-bnx2
         firmware-bnx2x
         firmware-brcm80211
         firmware-cavium
         firmware-cirrus
         firmware-intel-graphics
         firmware-intel-misc
         firmware-intel-sound
         firmware-iwlwifi
         firmware-libertas
         firmware-marvell-prestera
         firmware-mediatek
         firmware-misc-nonfree
         firmware-myricom
         firmware-netronome
         firmware-netxen
         firmware-nvidia-graphics
         firmware-qlogic
         firmware-realtek
         firmware-siano
         firmware-ast
         firmware-ath9k-htc
         firmware-carl9170
         firmware-zd1211
         atmel-firmware
         bluez-firmware

# CLI installer (Task 8.2): the product-aware snosi-install program plus the
# committed dev signed-index pubring and MOK certificate it verifies
# against -- single canonical copies (docs/native-ab-contracts.md §7,
# shared/native-ab/keys/README.md), never duplicated into this tree.
ExtraTrees=%D/shared/native-installer/tree
ExtraTrees=%D/shared/native-ab/keys/import-pubring.gpg:/usr/lib/snosi/os-update-pubring.gpg
ExtraTrees=%D/shared/native-ab/keys/mok-2026.crt:/usr/lib/snosi/mok.crt
# The GTK4 kiosk setup wizard (T2/T3, docs/plans/2026-07-17-graphical-
# installer-plan.md) installs as a plain directory; /usr/bin/snosi-setup is
# a symlink shipped in tree/ pointing into it.
ExtraTrees=%D/shared/native-installer/setup-gui:/usr/lib/snosi-setup

# Graphical setup stack (T3): cage kiosk compositor + GTK4/libadwaita via
# Python/GI, Mesa's software (llvmpipe) fallback so virtio/simple-framebuffer
# machines render, a real UI font, and the picker data files snosi-setup's
# locale/keyboard/timezone pages read (locales: /usr/share/i18n/SUPPORTED;
# xkb-data: evdev.lst; tzdata: zoneinfo) -- without them the pickers silently
# degrade to a tiny embedded list. Every entry explicit, per the forky-drift
# rule ("mount" incident, 2026-07-16).
Packages=cage
         gir1.2-gtk-4.0
         gir1.2-adw-1
         python3-gi
         fonts-cantarell
         libgl1-mesa-dri
         libegl-mesa0
         locales
         xkb-data
         tzdata

PostInstallationScripts=%D/shared/native-installer/postinst/mkosi.postinst.chroot
