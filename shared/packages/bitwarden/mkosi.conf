
[Content]
# Runtime deps from the Bitwarden .deb's Depends (installed via mkosi's
# host-side apt so dpkg -i in the postinst can install the .deb; the sysext
# build's base chroot has none of them).
Packages=libnotify4
         libnss3
         libxss1
         libxtst6
