# Sundog uses Plasma's Breeze theme from plymouth-theme-breeze. This conffile
# is delivered as an ExtraTree after dpkg, avoiding a noninteractive conffile
# prompt during package installation.
[Daemon]
Theme=breeze
ShowDelay=0
DeviceTimeout=8
